global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "auth_service"
    static_configs:
      # host.docker.internal — работает на macOS и Windows.
      # На Linux замените на: localhost:8000
      - targets: ["host.docker.internal:8000"]
